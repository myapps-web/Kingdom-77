# Kingdom-77 v3.0 Requirements
# ============================

# Core Bot
discord.py==2.6.4
python-dotenv==1.0.0

# Translation
deep-translator==1.11.4
langdetect==1.0.9

# Database (MongoDB)
motor==3.3.2              # Async MongoDB driver
pymongo==4.6.1            # MongoDB sync driver (for motor)
dnspython==2.4.2          # Required for MongoDB Atlas

# Cache (Redis)
redis==5.0.1              # Redis client with async support

# Payment Processing (v3.8)
stripe==7.3.0             # Stripe payment integration (international)
requests>=2.31.0          # HTTP client for Moyasar API (Saudi Arabia/GCC)

# Image Generation (v3.7)
Pillow==10.1.0            # Image processing for level cards
aiohttp==3.9.1            # Async HTTP client for avatar downloads

# Email Notifications (v3.7)
resend==0.8.0             # Email service for notifications

# Web Server & Dashboard
flask==3.0.0              # Keep-Alive server
fastapi==0.104.1          # FastAPI for dashboard API
uvicorn[standard]==0.24.0 # ASGI server for FastAPI
pydantic==2.5.0           # Data validation

# Utilities
beautifulsoup4>=4.9.1
requests>=2.23.0
parsedatetime>=2.6       # Time parsing for giveaways
